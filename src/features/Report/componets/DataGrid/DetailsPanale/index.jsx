import React from 'react';

const DetailsPanel = (params) => {
  // Customize this function to render the details panel content
  return (
    <div>
      <p>Details Panel for Row ID: {params.id}</p>
    </div>
  );
};

export default DetailsPanel;
